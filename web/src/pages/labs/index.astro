---
/**
 * Labs Homepage - Experimental Features
 *
 * Displays grid of 100+ experimental innovations including:
 * - Multi-modal clones
 * - Real-time voice conversation
 * - Clone memory
 * - Clone personality evolution
 * - Clone swarm
 * - Clone API
 * - Clone marketplace
 *
 * Ontology Mapping:
 * - GROUPS: Experiments scoped by groupId
 * - PEOPLE: User authorization for labs access
 * - THINGS: experiments (type: "experiment")
 * - CONNECTIONS: enabled_experiment (user → experiment)
 * - EVENTS: experiment_enabled, experiment_disabled, experiment_used
 * - KNOWLEDGE: Experiment descriptions and feedback
 */

import Layout from '../../layouts/Layout.astro';
import LabsGrid from '../../components/labs/LabsGrid';

const title = 'Experimental Features Lab';
const description = 'Explore 100+ cutting-edge AI clone innovations before they go mainstream';
---

<Layout title={title} description={description}>
  <div class="min-h-screen bg-gradient-to-br from-purple-50 via-blue-50 to-indigo-50">
    <!-- Hero Section -->
    <div class="relative overflow-hidden">
      <!-- Gradient background -->
      <div class="absolute inset-0 bg-gradient-to-r from-purple-600 to-blue-600 opacity-10"></div>

      <!-- Animated particles -->
      <div class="absolute inset-0 overflow-hidden pointer-events-none">
        {Array.from({ length: 20 }).map((_, i) => (
          <div
            key={i}
            class="absolute w-2 h-2 bg-blue-500 rounded-full opacity-20 animate-float"
            style={{
              left: `${Math.random() * 100}%`,
              top: `${Math.random() * 100}%`,
              animationDelay: `${Math.random() * 5}s`,
              animationDuration: `${5 + Math.random() * 10}s`,
            }}
          />
        ))}
      </div>

      <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
        <div class="text-center">
          <!-- Icon -->
          <div class="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-r from-purple-600 to-blue-600 rounded-2xl mb-6">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
            </svg>
          </div>

          <!-- Title -->
          <h1 class="text-5xl font-bold text-gray-900 mb-4">
            Experimental Features Lab
          </h1>

          <!-- Description -->
          <p class="text-xl text-gray-600 max-w-3xl mx-auto mb-8">
            Welcome to the innovation playground. Explore 100+ cutting-edge AI clone features
            before they go mainstream. Enable experiments, provide feedback, and help shape the future.
          </p>

          <!-- Stats -->
          <div class="flex items-center justify-center gap-8 mb-8">
            <div class="text-center">
              <div class="text-3xl font-bold text-purple-600">100+</div>
              <div class="text-sm text-gray-500">Experiments</div>
            </div>
            <div class="text-center">
              <div class="text-3xl font-bold text-blue-600">7</div>
              <div class="text-sm text-gray-500">Categories</div>
            </div>
            <div class="text-center">
              <div class="text-3xl font-bold text-indigo-600">∞</div>
              <div class="text-sm text-gray-500">Possibilities</div>
            </div>
          </div>

          <!-- Warning banner -->
          <div class="inline-flex items-center gap-2 px-6 py-3 bg-yellow-50 border border-yellow-200 rounded-full text-sm text-yellow-800">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
            </svg>
            <span>These are experimental features. Expect bugs and breaking changes.</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Main Content -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <!-- Filter/Search Bar -->
      <div class="mb-8">
        <div class="flex flex-col sm:flex-row gap-4">
          <div class="flex-1">
            <input
              type="text"
              id="search"
              placeholder="Search experiments..."
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
            />
          </div>
          <select
            id="category-filter"
            class="px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
          >
            <option value="">All Categories</option>
            <option value="multimodal">Multi-modal</option>
            <option value="realtime">Real-time</option>
            <option value="memory">Memory</option>
            <option value="learning">Learning</option>
            <option value="collaboration">Collaboration</option>
            <option value="integration">Integration</option>
            <option value="marketplace">Marketplace</option>
          </select>
          <select
            id="status-filter"
            class="px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
          >
            <option value="">All Status</option>
            <option value="active">Active</option>
            <option value="draft">Draft</option>
            <option value="archived">Archived</option>
          </select>
        </div>
      </div>

      <!-- Experiments Grid (React component) -->
      <LabsGrid client:load />

      <!-- Feature Highlights -->
      <div class="mt-16 grid md:grid-cols-3 gap-8">
        <!-- Feature 1 -->
        <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-200">
          <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z" />
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Innovation First</h3>
          <p class="text-gray-600 text-sm">
            Get early access to features months before they reach production. Shape the roadmap with your feedback.
          </p>
        </div>

        <!-- Feature 2 -->
        <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-200">
          <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4" />
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Granular Control</h3>
          <p class="text-gray-600 text-sm">
            Enable only the experiments you want. Toggle features on/off instantly without affecting your production setup.
          </p>
        </div>

        <!-- Feature 3 -->
        <div class="bg-white rounded-xl shadow-sm p-6 border border-gray-200">
          <div class="w-12 h-12 bg-indigo-100 rounded-lg flex items-center justify-center mb-4">
            <svg class="w-6 h-6 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Safe Experimentation</h3>
          <p class="text-gray-600 text-sm">
            Labs run in isolated environments. Nothing breaks production. Experiment fearlessly.
          </p>
        </div>
      </div>

      <!-- CTA Section -->
      <div class="mt-16 bg-gradient-to-r from-purple-600 to-blue-600 rounded-2xl p-8 text-center">
        <h2 class="text-3xl font-bold text-white mb-4">
          Have an Idea for an Experiment?
        </h2>
        <p class="text-purple-100 mb-6 max-w-2xl mx-auto">
          We're always looking for new experiments to try. Submit your idea and it might
          become the next innovation in the lab.
        </p>
        <button class="px-6 py-3 bg-white text-purple-600 font-semibold rounded-lg hover:bg-gray-100 transition-colors">
          Submit Experiment Idea
        </button>
      </div>
    </div>
  </div>
</Layout>

<style>
  @keyframes float {
    0%, 100% {
      transform: translateY(0) translateX(0);
    }
    25% {
      transform: translateY(-20px) translateX(10px);
    }
    50% {
      transform: translateY(-10px) translateX(-10px);
    }
    75% {
      transform: translateY(-30px) translateX(5px);
    }
  }

  .animate-float {
    animation: float linear infinite;
  }
</style>

<script>
  // Search functionality
  const searchInput = document.getElementById('search') as HTMLInputElement;
  const categoryFilter = document.getElementById('category-filter') as HTMLSelectElement;
  const statusFilter = document.getElementById('status-filter') as HTMLSelectElement;

  searchInput?.addEventListener('input', () => {
    // Trigger search (handled by React component via custom events)
    window.dispatchEvent(new CustomEvent('labs:search', {
      detail: { query: searchInput.value }
    }));
  });

  categoryFilter?.addEventListener('change', () => {
    window.dispatchEvent(new CustomEvent('labs:filter', {
      detail: { category: categoryFilter.value }
    }));
  });

  statusFilter?.addEventListener('change', () => {
    window.dispatchEvent(new CustomEvent('labs:filter', {
      detail: { status: statusFilter.value }
    }));
  });
</script>
