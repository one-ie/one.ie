---
---

<Layout title="Deployment Metrics - Chart Components Demo" sidebarInitialCollapsed={true}>
  <!-- Custom Gradient Animation Styles -->
  <style is:global>
    @keyframes gradient {
      0%, 100% {
        background-position: 0% 50%;
      }
      50% {
        background-position: 100% 50%;
      }
    }

    .animate-gradient {
      background-size: 200% 200%;
      animation: gradient 3s ease infinite;
    }

    @keyframes float {
      0%, 100% {
        transform: translateY(0px);
      }
      50% {
        transform: translateY(-20px);
      }
    }

    .animate-float {
      animation: float 3s ease-in-out infinite;
    }

    @keyframes shimmer {
      0% {
        transform: translateX(-100%);
      }
      100% {
        transform: translateX(100%);
      }
    }

    .animate-shimmer {
      animation: shimmer 2s infinite;
    }

    /* Accessibility: Respect reduced motion preference */
    @media (prefers-reduced-motion: reduce) {
      * {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
      }
    }
  </style>

  <div class="min-h-screen px-4 py-12 sm:px-6 md:px-8 relative overflow-hidden">
    <!-- Animated Background Orbs -->
    <div class="absolute -top-40 -right-40 h-96 w-96 bg-primary/10 rounded-full blur-3xl animate-pulse pointer-events-none" aria-hidden="true"></div>
    <div class="absolute -bottom-40 -left-40 h-96 w-96 bg-purple-500/10 rounded-full blur-3xl animate-pulse delay-700 pointer-events-none" aria-hidden="true"></div>

    <div class="mx-auto max-w-7xl space-y-20 relative z-10">
      {/* Header Section */}
      <div class="space-y-8 text-center">
        <Badge variant="secondary" className="inline-flex items-center gap-2 text-lg font-bold px-8 py-3 shadow-lg hover:scale-105 transition-transform duration-300">
          <Sparkles className="h-6 w-6 animate-pulse" />
          Component Gallery
        </Badge>

        <h1 class="text-6xl md:text-7xl font-black tracking-tight bg-gradient-to-r from-primary via-purple-500 to-primary bg-clip-text text-transparent animate-gradient">
          Deployment Metrics
          <br />
          Chart Components
        </h1>

        <p class="mx-auto max-w-3xl text-xl md:text-2xl leading-relaxed text-muted-foreground">
          Beautiful, <strong class="text-foreground">high-converting</strong> React chart components for showcasing deployment and performance metrics.
          <br />
          <span class="font-bold text-green-600">Zero dependencies.</span>{' '}
          <span class="font-bold text-blue-600">Fully typed.</span>{' '}
          <span class="font-bold text-purple-600">Production ready.</span>
        </p>

        <!-- Live Demo Badge -->
        <div class="pt-4">
          <Badge className="bg-gradient-to-r from-green-500 to-emerald-500 text-white text-base px-6 py-2 shadow-xl shadow-green-500/30">
            <span class="relative flex h-3 w-3 mr-2">
              <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-white opacity-75"></span>
              <span class="relative inline-flex rounded-full h-3 w-3 bg-white"></span>
            </span>
            Live Interactive Demo
          </Badge>
        </div>
      </div>

      {/* Component 1: Performance Metrics */}
      <section class="space-y-8">
        <div class="space-y-3">
          <Badge variant="outline" className="text-sm font-semibold px-4 py-1">Component 1 of 8</Badge>
          <h2 class="text-4xl font-black bg-gradient-to-r from-blue-600 via-cyan-500 to-blue-600 bg-clip-text text-transparent">
            Performance Metrics
          </h2>
          <p class="text-lg text-muted-foreground max-w-2xl">
            4-card layout with <strong class="text-foreground">animated count-up numbers</strong>, gradient backgrounds, and mini visualizations.
            Watch the metrics come alive!
          </p>
        </div>

        <div class="rounded-2xl border-2 border-primary/20 bg-gradient-to-br from-primary/5 via-background to-background backdrop-blur-xl p-8 md:p-12 shadow-2xl">
          <PerformanceMetrics client:load columns={2} animate={true} />
        </div>

        <div class="rounded-lg border border-border/50 bg-background/50 p-4">
          <div class="flex items-start gap-2 text-sm">
            <Code className="h-5 w-5 text-primary flex-shrink-0 mt-0.5" />
            <pre class="overflow-x-auto flex-1"><code>{`import { PerformanceMetrics } from '@/components/charts/PerformanceMetrics';

export default function Page() {
  return (
    <PerformanceMetrics
      columns={2}
      showDescriptions={true}
    />
  );
}`}</code></pre>
          </div>
        </div>
      </section>

      {/* Component 2: Deployment Speed */}
      <section class="space-y-8">
        <div class="space-y-3">
          <Badge variant="outline" className="text-sm font-semibold px-4 py-1">Component 2 of 8</Badge>
          <h2 class="text-4xl font-black bg-gradient-to-r from-purple-600 via-pink-500 to-purple-600 bg-clip-text text-transparent">
            Deployment Speed
          </h2>
          <p class="text-lg text-muted-foreground max-w-2xl">
            <strong class="text-foreground">Sequentially animated</strong> 4-stage pipeline that plays through the entire deployment process.
            Each stage glows when active!
          </p>
        </div>

        <div class="rounded-2xl border-2 border-purple-500/20 bg-gradient-to-br from-purple-500/5 via-background to-background backdrop-blur-xl p-8 md:p-12 shadow-2xl">
          <DeploymentSpeed client:load timestamp="Nov 6, 2025" animate={true} />
        </div>

        <div class="rounded-lg border border-border/50 bg-background/50 p-4">
          <div class="flex items-start gap-2 text-sm">
            <Code className="h-5 w-5 text-primary flex-shrink-0 mt-0.5" />
            <pre class="overflow-x-auto flex-1"><code>{`import { DeploymentSpeed } from '@/components/charts/DeploymentSpeed';

export default function Page() {
  return (
    <DeploymentSpeed
      showDetails={true}
      timestamp="Nov 6, 2025"
    />
  );
}`}</code></pre>
          </div>
        </div>
      </section>

      {/* Component 3: Pricing Comparison */}
      <section class="space-y-8">
        <div class="space-y-3">
          <Badge variant="outline" className="text-sm font-semibold px-4 py-1">Component 3 of 8</Badge>
          <h2 class="text-4xl font-black bg-gradient-to-r from-green-600 via-emerald-500 to-green-600 bg-clip-text text-transparent">
            Pricing Comparison
          </h2>
          <p class="text-lg text-muted-foreground max-w-2xl">
            Side-by-side comparison with <strong class="text-foreground">animated price counters</strong>, progress bars, and confetti effects.
            Watch the savings add up!
          </p>
        </div>

        <div class="rounded-2xl border-2 border-green-500/20 bg-gradient-to-br from-green-500/5 via-background to-background backdrop-blur-xl p-8 md:p-12 shadow-2xl">
          <PricingComparison client:load showDetails={true} animate={true} />
        </div>

        <div class="rounded-lg border border-border/50 bg-background/50 p-4">
          <div class="flex items-start gap-2 text-sm">
            <Code className="h-5 w-5 text-primary flex-shrink-0 mt-0.5" />
            <pre class="overflow-x-auto flex-1"><code>{`import { PricingComparison } from '@/components/charts/PricingComparison';

export default function Page() {
  return (
    <PricingComparison
      showDetails={true}
    />
  );
}`}</code></pre>
          </div>
        </div>
      </section>

      {/* Component 4: Included Features */}
      <section class="space-y-8">
        <div class="space-y-3">
          <Badge variant="outline" className="text-sm font-semibold px-4 py-1">Component 4 of 8</Badge>
          <h2 class="text-4xl font-black bg-gradient-to-r from-orange-600 via-red-500 to-orange-600 bg-clip-text text-transparent">
            Included Features
          </h2>
          <p class="text-lg text-muted-foreground max-w-2xl">
            Feature cards with <strong class="text-foreground">staggered entrance animations</strong>, sparkle effects, and rotating icons on hover.
            Premium UI that converts!
          </p>
        </div>

        <div class="rounded-2xl border-2 border-orange-500/20 bg-gradient-to-br from-orange-500/5 via-background to-background backdrop-blur-xl p-8 md:p-12 shadow-2xl">
          <IncludedFeatures client:load columns={3} showDescriptions={true} animate={true} />
        </div>

        <div class="rounded-lg border border-border/50 bg-background/50 p-4">
          <div class="flex items-start gap-2 text-sm">
            <Code className="h-5 w-5 text-primary flex-shrink-0 mt-0.5" />
            <pre class="overflow-x-auto flex-1"><code>{`import { IncludedFeatures } from '@/components/charts/IncludedFeatures';

export default function Page() {
  return (
    <IncludedFeatures
      columns={3}
      showDescriptions={true}
      showFooter={true}
    />
  );
}`}</code></pre>
          </div>
        </div>
      </section>

      {/* Component 5: Network Topology */}
      <section class="space-y-8">
        <div class="space-y-3">
          <Badge variant="outline" className="text-sm font-semibold px-4 py-1">Component 5 of 8</Badge>
          <h2 class="text-4xl font-black bg-gradient-to-r from-cyan-600 via-blue-500 to-cyan-600 bg-clip-text text-transparent">
            Network Topology
          </h2>
          <p class="text-lg text-muted-foreground max-w-2xl">
            <strong class="text-foreground">Global edge network visualization</strong> with live connection animations, regional status, and latency metrics.
            See your infrastructure at a glance!
          </p>
        </div>

        <div class="rounded-2xl border-2 border-cyan-500/20 bg-gradient-to-br from-cyan-500/5 via-background to-background backdrop-blur-xl p-8 md:p-12 shadow-2xl">
          <NetworkTopology client:visible showDetails={true} animate={true} viewMode="grid" />
        </div>

        <div class="rounded-lg border border-border/50 bg-background/50 p-4">
          <div class="flex items-start gap-2 text-sm">
            <Code className="h-5 w-5 text-primary flex-shrink-0 mt-0.5" />
            <pre class="overflow-x-auto flex-1"><code>{`import { NetworkTopology } from '@/components/charts/NetworkTopology';

export default function Page() {
  return (
    <NetworkTopology
      showDetails={true}
      animate={true}
      viewMode="grid"
    />
  );
}`}</code></pre>
          </div>
        </div>
      </section>

      {/* Component 6: Build Timeline */}
      <section class="space-y-8">
        <div class="space-y-3">
          <Badge variant="outline" className="text-sm font-semibold px-4 py-1">Component 6 of 8</Badge>
          <h2 class="text-4xl font-black bg-gradient-to-r from-indigo-600 via-purple-500 to-indigo-600 bg-clip-text text-transparent">
            Build Timeline
          </h2>
          <p class="text-lg text-muted-foreground max-w-2xl">
            <strong class="text-foreground">Interactive build process visualization</strong> with real-time progress tracking, console output, and step-by-step breakdowns.
            Watch your builds come to life!
          </p>
        </div>

        <div class="rounded-2xl border-2 border-indigo-500/20 bg-gradient-to-br from-indigo-500/5 via-background to-background backdrop-blur-xl p-8 md:p-12 shadow-2xl">
          <BuildTimeline client:visible showDetails={true} animate={true} autoPlay={true} />
        </div>

        <div class="rounded-lg border border-border/50 bg-background/50 p-4">
          <div class="flex items-start gap-2 text-sm">
            <Code className="h-5 w-5 text-primary flex-shrink-0 mt-0.5" />
            <pre class="overflow-x-auto flex-1"><code>{`import { BuildTimeline } from '@/components/charts/BuildTimeline';

export default function Page() {
  return (
    <BuildTimeline
      showDetails={true}
      animate={true}
      autoPlay={true}
    />
  );
}`}</code></pre>
          </div>
        </div>
      </section>

      {/* Component 7: Resource Usage */}
      <section class="space-y-8">
        <div class="space-y-3">
          <Badge variant="outline" className="text-sm font-semibold px-4 py-1">Component 7 of 8</Badge>
          <h2 class="text-4xl font-black bg-gradient-to-r from-teal-600 via-cyan-500 to-teal-600 bg-clip-text text-transparent">
            Resource Usage
          </h2>
          <p class="text-lg text-muted-foreground max-w-2xl">
            <strong class="text-foreground">Real-time infrastructure monitoring</strong> with CPU, memory, storage, and bandwidth metrics.
            Auto-scaling visualizations included!
          </p>
        </div>

        <div class="rounded-2xl border-2 border-teal-500/20 bg-gradient-to-br from-teal-500/5 via-background to-background backdrop-blur-xl p-8 md:p-12 shadow-2xl">
          <ResourceUsage client:visible showDetails={true} animate={true} viewMode="cards" />
        </div>

        <div class="rounded-lg border border-border/50 bg-background/50 p-4">
          <div class="flex items-start gap-2 text-sm">
            <Code className="h-5 w-5 text-primary flex-shrink-0 mt-0.5" />
            <pre class="overflow-x-auto flex-1"><code>{`import { ResourceUsage } from '@/components/charts/ResourceUsage';

export default function Page() {
  return (
    <ResourceUsage
      showDetails={true}
      animate={true}
      viewMode="cards"
    />
  );
}`}</code></pre>
          </div>
        </div>
      </section>

      {/* Component 8: Traffic Analytics */}
      <section class="space-y-8">
        <div class="space-y-3">
          <Badge variant="outline" className="text-sm font-semibold px-4 py-1">Component 8 of 8</Badge>
          <h2 class="text-4xl font-black bg-gradient-to-r from-purple-600 via-indigo-500 to-purple-600 bg-clip-text text-transparent">
            Traffic Analytics
          </h2>
          <p class="text-lg text-muted-foreground max-w-2xl">
            <strong class="text-foreground">Live visitor tracking and engagement metrics</strong> with geographic distribution, device breakdowns, and top pages analysis.
            Know your audience!
          </p>
        </div>

        <div class="rounded-2xl border-2 border-purple-500/20 bg-gradient-to-br from-purple-500/5 via-background to-background backdrop-blur-xl p-8 md:p-12 shadow-2xl">
          <TrafficAnalytics client:visible showDetails={true} animate={true} timeRange="24h" />
        </div>

        <div class="rounded-lg border border-border/50 bg-background/50 p-4">
          <div class="flex items-start gap-2 text-sm">
            <Code className="h-5 w-5 text-primary flex-shrink-0 mt-0.5" />
            <pre class="overflow-x-auto flex-1"><code>{`import { TrafficAnalytics } from '@/components/charts/TrafficAnalytics';

export default function Page() {
  return (
    <TrafficAnalytics
      showDetails={true}
      animate={true}
      timeRange="24h"
    />
  );
}`}</code></pre>
          </div>
        </div>
      </section>

      {/* Component Specifications */}
      <section class="space-y-6 border-t border-border/50 pt-12">
        <h2 class="text-3xl font-bold tracking-tight">Component Specifications</h2>

        <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
          {/* PerformanceMetrics */}
          <div class="rounded-lg border border-border/50 bg-card/50 backdrop-blur p-6 space-y-4">
            <h3 class="font-bold text-lg">PerformanceMetrics</h3>
            <div class="space-y-3 text-sm">
              <div>
                <p class="font-semibold text-foreground">Props</p>
                <pre class="text-xs bg-background/50 p-2 rounded mt-1 overflow-x-auto"><code>{`columns?: 1 | 2 | 3 | 4
showDescriptions?: boolean
animate?: boolean`}</code></pre>
              </div>
              <div>
                <p class="font-semibold text-foreground">Size</p>
                <p class="text-muted-foreground">~2.8 KB gzipped</p>
              </div>
              <div>
                <p class="font-semibold text-foreground">Best For</p>
                <p class="text-muted-foreground">Hero metrics dashboard</p>
              </div>
            </div>
          </div>

          {/* DeploymentSpeed */}
          <div class="rounded-lg border border-border/50 bg-card/50 backdrop-blur p-6 space-y-4">
            <h3 class="font-bold text-lg">DeploymentSpeed</h3>
            <div class="space-y-3 text-sm">
              <div>
                <p class="font-semibold text-foreground">Props</p>
                <pre class="text-xs bg-background/50 p-2 rounded mt-1 overflow-x-auto"><code>{`showDetails?: boolean
timestamp?: string
animate?: boolean`}</code></pre>
              </div>
              <div>
                <p class="font-semibold text-foreground">Size</p>
                <p class="text-muted-foreground">~3.2 KB gzipped</p>
              </div>
              <div>
                <p class="font-semibold text-foreground">Best For</p>
                <p class="text-muted-foreground">CI/CD pipeline visualization</p>
              </div>
            </div>
          </div>

          {/* PricingComparison */}
          <div class="rounded-lg border border-border/50 bg-card/50 backdrop-blur p-6 space-y-4">
            <h3 class="font-bold text-lg">PricingComparison</h3>
            <div class="space-y-3 text-sm">
              <div>
                <p class="font-semibold text-foreground">Props</p>
                <pre class="text-xs bg-background/50 p-2 rounded mt-1 overflow-x-auto"><code>{`showDetails?: boolean
animate?: boolean`}</code></pre>
              </div>
              <div>
                <p class="font-semibold text-foreground">Size</p>
                <p class="text-muted-foreground">~2.5 KB gzipped</p>
              </div>
              <div>
                <p class="font-semibold text-foreground">Best For</p>
                <p class="text-muted-foreground">Pricing comparisons</p>
              </div>
            </div>
          </div>

          {/* IncludedFeatures */}
          <div class="rounded-lg border border-border/50 bg-card/50 backdrop-blur p-6 space-y-4">
            <h3 class="font-bold text-lg">IncludedFeatures</h3>
            <div class="space-y-3 text-sm">
              <div>
                <p class="font-semibold text-foreground">Props</p>
                <pre class="text-xs bg-background/50 p-2 rounded mt-1 overflow-x-auto"><code>{`columns?: 1 | 2 | 3
showFooter?: boolean
showDescriptions?: boolean
animate?: boolean`}</code></pre>
              </div>
              <div>
                <p class="font-semibold text-foreground">Size</p>
                <p class="text-muted-foreground">~3.1 KB gzipped</p>
              </div>
              <div>
                <p class="font-semibold text-foreground">Best For</p>
                <p class="text-muted-foreground">Feature showcases</p>
              </div>
            </div>
          </div>

          {/* NetworkTopology */}
          <div class="rounded-lg border border-border/50 bg-card/50 backdrop-blur p-6 space-y-4">
            <h3 class="font-bold text-lg">NetworkTopology</h3>
            <div class="space-y-3 text-sm">
              <div>
                <p class="font-semibold text-foreground">Props</p>
                <pre class="text-xs bg-background/50 p-2 rounded mt-1 overflow-x-auto"><code>{`showDetails?: boolean
animate?: boolean
viewMode?: 'map' | 'grid'`}</code></pre>
              </div>
              <div>
                <p class="font-semibold text-foreground">Size</p>
                <p class="text-muted-foreground">~4.5 KB gzipped</p>
              </div>
              <div>
                <p class="font-semibold text-foreground">Best For</p>
                <p class="text-muted-foreground">Infrastructure visualization</p>
              </div>
            </div>
          </div>

          {/* BuildTimeline */}
          <div class="rounded-lg border border-border/50 bg-card/50 backdrop-blur p-6 space-y-4">
            <h3 class="font-bold text-lg">BuildTimeline</h3>
            <div class="space-y-3 text-sm">
              <div>
                <p class="font-semibold text-foreground">Props</p>
                <pre class="text-xs bg-background/50 p-2 rounded mt-1 overflow-x-auto"><code>{`showDetails?: boolean
animate?: boolean
autoPlay?: boolean`}</code></pre>
              </div>
              <div>
                <p class="font-semibold text-foreground">Size</p>
                <p class="text-muted-foreground">~4.2 KB gzipped</p>
              </div>
              <div>
                <p class="font-semibold text-foreground">Best For</p>
                <p class="text-muted-foreground">Build process tracking</p>
              </div>
            </div>
          </div>

          {/* ResourceUsage */}
          <div class="rounded-lg border border-border/50 bg-card/50 backdrop-blur p-6 space-y-4">
            <h3 class="font-bold text-lg">ResourceUsage</h3>
            <div class="space-y-3 text-sm">
              <div>
                <p class="font-semibold text-foreground">Props</p>
                <pre class="text-xs bg-background/50 p-2 rounded mt-1 overflow-x-auto"><code>{`showDetails?: boolean
animate?: boolean
viewMode?: 'cards' | 'list' | 'compact'`}</code></pre>
              </div>
              <div>
                <p class="font-semibold text-foreground">Size</p>
                <p class="text-muted-foreground">~4.0 KB gzipped</p>
              </div>
              <div>
                <p class="font-semibold text-foreground">Best For</p>
                <p class="text-muted-foreground">System monitoring</p>
              </div>
            </div>
          </div>

          {/* TrafficAnalytics */}
          <div class="rounded-lg border border-border/50 bg-card/50 backdrop-blur p-6 space-y-4">
            <h3 class="font-bold text-lg">TrafficAnalytics</h3>
            <div class="space-y-3 text-sm">
              <div>
                <p class="font-semibold text-foreground">Props</p>
                <pre class="text-xs bg-background/50 p-2 rounded mt-1 overflow-x-auto"><code>{`showDetails?: boolean
animate?: boolean
timeRange?: '24h' | '7d' | '30d'`}</code></pre>
              </div>
              <div>
                <p class="font-semibold text-foreground">Size</p>
                <p class="text-muted-foreground">~4.8 KB gzipped</p>
              </div>
              <div>
                <p class="font-semibold text-foreground">Best For</p>
                <p class="text-muted-foreground">Analytics dashboards</p>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Features Overview */}
      <section class="space-y-6 border-t border-border/50 pt-12">
        <h2 class="text-3xl font-bold tracking-tight">Features</h2>

        <div class="grid gap-6 md:grid-cols-2">
          <div class="space-y-4">
            <h3 class="font-semibold text-lg">Design</h3>
            <ul class="space-y-2 text-sm text-muted-foreground">
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                Premium gradients & animations
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                Dark mode support built-in
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                Tailwind v4 CSS variables
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                Accessible WCAG 2.1 AA
              </li>
            </ul>
          </div>

          <div class="space-y-4">
            <h3 class="font-semibold text-lg">Performance</h3>
            <ul class="space-y-2 text-sm text-muted-foreground">
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                &lt; 5KB gzipped each
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                No external chart libraries
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                Islands architecture ready
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                Lighthouse 100 compatible
              </li>
            </ul>
          </div>

          <div class="space-y-4">
            <h3 class="font-semibold text-lg">Developer Experience</h3>
            <ul class="space-y-2 text-sm text-muted-foreground">
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                100% TypeScript strict mode
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                Fully documented props
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                Zero setup required
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                React 19 compatible
              </li>
            </ul>
          </div>

          <div class="space-y-4">
            <h3 class="font-semibold text-lg">Customization</h3>
            <ul class="space-y-2 text-sm text-muted-foreground">
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                All props are optional
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                Easy to extend & customize
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                Theme-aware colors
              </li>
              <li class="flex items-center gap-2">
                <span class="text-primary">✓</span>
                Responsive layouts
              </li>
            </ul>
          </div>
        </div>
      </section>

      {/* Usage Tips */}
      <section class="space-y-6 border-t border-border/50 pt-12">
        <h2 class="text-3xl font-bold tracking-tight">Usage Tips</h2>

        <div class="grid gap-6 md:grid-cols-3">
          <div class="rounded-lg border border-border/50 bg-card/50 backdrop-blur p-6 space-y-3">
            <h3 class="font-semibold">Islands Architecture</h3>
            <p class="text-sm text-muted-foreground">
              Use <code class="bg-background/50 px-1.5 py-0.5 rounded text-xs">client:load</code> for above-the-fold charts,
              <code class="bg-background/50 px-1.5 py-0.5 rounded text-xs ml-1">client:visible</code> for below-the-fold.
            </p>
          </div>

          <div class="rounded-lg border border-border/50 bg-card/50 backdrop-blur p-6 space-y-3">
            <h3 class="font-semibold">Dark Mode</h3>
            <p class="text-sm text-muted-foreground">
              All components automatically support dark mode via next-themes.
              No additional configuration needed.
            </p>
          </div>

          <div class="rounded-lg border border-border/50 bg-card/50 backdrop-blur p-6 space-y-3">
            <h3 class="font-semibold">Responsive Design</h3>
            <p class="text-sm text-muted-foreground">
              Mobile-first approach. All components are fully responsive.
              Stack to single column on mobile by default.
            </p>
          </div>
        </div>
      </section>
    </div>
  </div>
</Layout>
