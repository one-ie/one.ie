---
const url = new URL(Astro.request.url);
const _token = url.searchParams.get("token") || "";
---

<Layout title="Reset Password - ONE">
  <div class="flex items-center justify-center min-h-[calc(100vh-8rem)] px-4">
    <ResetPasswordPage token={token} client:load />
  </div>
</Layout>
